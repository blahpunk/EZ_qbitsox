# Web service
PORT=7272

# Optional: provide a stable Fernet key for encrypted settings.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
EZ_QBITSOX_SECRET_KEY=CHANGE_ME_OR_LEAVE_UNSET
